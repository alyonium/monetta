## 2026-08-25 20:57 - US-001
- Added Vitest (`npm test` = `vitest run`) and English i18next catalog. App, router, and Budget stub unchanged.
- **Learnings for future iterations:**
    - Patterns discovered: tests live in `src/**/*.test.ts`; import `describe`/`it`/`expect` from `vitest` (no globals). i18n instance and `t` are in `src/i18n`; later UI uses `useTranslation()`.
    - Gotchas encountered: separate `vitest.config.ts` so tests do not load `vite-plugin-pwa`. Unknown i18n keys fail typecheck; tests use `@ts-expect-error`. Do not use `baseUrl` in `tsconfig.app.json`; map `@/*` to `./src/*` in `paths`.
    - Useful context: interpolation is `{{name}}`. Seed keys are `app.name` and `app.hello`. Add new user-facing strings to `src/i18n/en.ts`.
---

## 2026-08-26 18:00 - US-002
- Added `normalizeBackendUrl`, localStorage helpers for token/URL, and `configureApiClient` (called from `main.tsx`). Generated `src/api` left untouched.
- **Learnings for future iterations:**
    - Patterns discovered: generated `client` is a singleton in `src/api/client.gen.ts`; configure it from `src/helpers` via `setConfig`. SDK paths are `/v1/...`, so baseUrl is `{origin}/api`, not `{origin}/api/v1`.
    - Gotchas encountered: Vitest `environment: 'node'` has no `localStorage` — stub with `vi.stubGlobal`. `setConfig` merges headers; restore tests with `Authorization`/`Accept` set to `null`. Missing scheme needs `https://` or `new URL` throws. Do not put `/v1` in baseUrl or requests become `/api/v1/v1/...`. Subpath Firefly installs are out of scope for V1.
    - Useful context: storage keys are `monetta.token` and `monetta.backendUrl`. Normalize on write only; `configureApiClient` normalizes again before `setConfig`. `clearAuth*` belongs to US-039. Login UI is US-003; invalid URLs surface there as a backend URL error.
---

## 2026-08-27 20:16 - US-003
- Login screen at `/monetta/login`: Token + Backend URL, `getCurrentUser` check, persist only on success, then Budget. Empty fields and 401/403 vs network/CORS/non-API mapped to field errors.
- **Learnings for future iterations:**
    - Patterns discovered: screens live in `src/modules/<name>/` with `helpers/` and `tests/`. CSS modules sit next to the component. UI strings go through `useTranslation()` and `src/i18n/en.ts`.
    - Gotchas encountered: verify with `getCurrentUser` (`/v1/about/user`), not `getAbout`. Keep `throwOnError` false; wrap in try/catch because HTML-as-JSON still throws. Persist only after success so failed login does not wipe saved creds. Vitest is `*.test.ts` + `environment: 'node'` — no component tests. Dev server may be on 5174 if 5173 is taken.
    - Useful context: `ROUTE.LOGIN` is `/monetta/login`. Auth guard and footer tabs are US-004. Logout/clear is US-039. Successful live login needs a Firefly instance that allows the app origin (CORS from localhost often looks like an invalid backend URL).
---

## 2026-08-28 13:50 - US-004
- Auth guard (`RequireAuth` + `hasAuthCredentials`) and footer tabs Budget / History / Analytics / Settings. Login stays without footer. Default path after auth is `/monetta/budget`.
- **Learnings for future iterations:**
    - Patterns discovered: shared chrome lives in `src/components` (`RequireAuth`, `AppLayout`). Page stubs stay one default-export file in `src/modules/<name>/`. Footer tabs are `FOOTER_TABS` in `src/components/constants.ts`, not in the router. Labels go through `nav.*` in `en.ts`.
    - Gotchas encountered: both token and backend URL must be non-empty after trim; empty `localStorage` strings are logged out. Nested routes: `RequireAuth` → `AppLayout` (`Outlet` + footer) → pages. Login is a sibling, not inside the layout. Do not bounce login to Budget if already signed in. Vitest is still `*.test.ts` + `node`, so no component tests for the guard.
    - Useful context: `AppLayout` is a `100dvh` column; `main` is `flex: 1; min-height: 0` so US-009 can fill the space above the footer. Phosphor icons: Wallet, ClockCounterClockwise, ChartPieSlice, Gear. Logout/clear is still US-039. Query provider (US-005) should wrap the authenticated tree, not login.
---

## 2026-08-31 20:47 - US-005
- TanStack Query + IndexedDB persist on the authenticated tree. Login stays outside the provider. Factories, persister key `monetta.queryCache`, restore then `invalidateQueries`.
- **Learnings for future iterations:**
    - Patterns discovered: Query provider is a route layout (`QueryPersistenceProvider` + `Outlet`) under `RequireAuth`, not around `App`. Client via `useState(() => createQueryClient())`. Persist uses custom `idb-keyval` persister, not localStorage.
    - Gotchas encountered: `gcTime` must be ≥ persist `maxAge` (both 24h) or hydration drops the snapshot. Persist only writes on cache added/removed/updated — empty cache does not create the `monetta.queryCache` key yet; `keyval-store` still opens on restore `get()`. Mock `idb-keyval` in node tests. Vite is on 5175 (`strictPort`).
    - Useful context: `QUERY_CACHE_KEY` / `createQueryPersister` are the same helpers US-039 should call on logout with `queryClient.clear()`. First `useQuery` is US-006 / US-008. Token and backend URL stay in localStorage.
---

## 2026-09-03 12:40 - US-006
- Added wallet currency helpers: `fetchPrimaryCurrency`, `fetchExchangeRates` (paged, latest per pair), and `convertToPrimary`. No UI and no `useQuery`.
- **Learnings for future iterations:**
    - Patterns discovered: shared currency types live in `src/helpers/currency/types.ts`; later Budget/Analytics import from `@/helpers/currency/...`. Fetchers mock `@/api/sdk.gen.ts` like login. Tests stay in `src/helpers/tests/`.
    - Gotchas encountered: `primaryCurrency` is a POST; read with `getPrimaryCurrency`. `result.data` is possibly undefined after `result.data?.data` — assign `payload` before reading `meta`. Rate is a string. Empty `data: []` is valid; missing `data` throws.
    - Useful context: US-014 wraps these fetchers in Query for parameters-bar totals. Do not round in the converter. Account cards stay in their own currency (US-010).
---

## 2026-09-03 14:42 - US-007
- Added Budget account model, Firefly mapping, and preference read/write helpers. No UI and no `useQuery`.
- **Learnings for future iterations:**
    - Patterns discovered: Budget types live in `src/modules/budget/types`; mapping and preferences in `src/modules/budget/helpers`; tests in `src/modules/budget/tests`. Same SDK mock as currency fetchers.
    - Gotchas encountered: `PolymorphicProperty` is `boolean | string | string[]`, not an object — appearance is a JSON string, order is a string array. PUT `updatePreference` overwrites and creates missing keys. Broken appearance JSON is skipped, missing `data` on list throws. Do not write appearance during mapping. `icon`/`color` stay `null` without a preference — no Wallet/`#4C6EF5` in the data layer.
    - Useful context: US-008 wraps `listAccount` + these helpers in `useBudgetAccounts`. Cards stay in account currency. UI fallback for missing appearance is US-010 / US-016. First appearance write is create/edit (US-017 / US-020). Reorder writes `monetta.accountOrder.<type.toLowerCase()>` (US-027).
---

## 2026-09-03 15:14 - US-007 review
- Mapper takes `Pick<AccountRead, 'id' | 'attributes'>`. One `AccountType`; order keys lowercased only on the Firefly wire. Appearance is `string | null`, not a Mantine default.
- **Learnings for future iterations:**
    - Patterns discovered: bucket with `grouped[mapped.type]`. Preference name `accountOrderPreferenceName(type)`.
    - Gotchas encountered: do not invent a looser Firefly account type for tests. Do not bake UI tokens into the model.
    - Useful context: shared Firefly page-loop waits for US-008 `listAccount`.
---

## 2026-09-04 16:45 - US-008
- Budget loads accounts via Query: shared `collectFireflyPages`, `fetchBudgetAccounts`, `useBudgetAccounts`, names on Budget. No layout chrome.
- **Learnings for future iterations:**
    - Patterns discovered: `collectFireflyPages` next to `configureApiClient`. First `useQuery` is `useBudgetAccounts` with key `['budget', 'accounts']`. `emptyAccountsByType` exported from `toBudgetAccounts`.
    - Gotchas encountered: do not pass `type`/`date` to `listAccount`. Do not set `placeholderData`. Page limits stay separate 50s. Without a Firefly PAT the UI shows `budget.errors.loadFailed`.
    - Useful context: US-009 reuses `budget.income` / `budget.current` / `budget.expense`. US-013 may add month to the query key. Mutations invalidate this key from US-017.
---

## 2026-09-04 17:18 - US-008 review
- Hook is a thin `useQuery`. Budget uses `data !== undefined`, not list lengths. `emptyAccountsByType` is private again.
- **Learnings for future iterations:**
    - Patterns discovered: keep `BudgetAccountsByBlock` keys (`INCOME` / `CURRENT` / `EXPENSE`) through Query to the page.
    - Gotchas encountered: `data ?? empty` hides persist cache vs cold start. Empty arrays plus refetch error must still show the cache.
    - Useful context: US-009+ should read `data[ACCOUNT_TYPE.*]`, not a renamed hook shape.
---

## 2026-09-04 19:15 - US-009
- Budget page shell: column fills `main`, ParametersBar stub, three AccountBlocks (`flex: 1`). Account names stay in the grid slot. `AccountNameList` removed.
- **Learnings for future iterations:**
    - Patterns discovered: CSS modules next to each Budget component. `AppLayout` `main` is `overflow: hidden`; the page column is `height: 100%`.
    - Gotchas encountered: keep `!data` for loading/error. Bar `expenses` vs block `expense`. Do not add MonthPicker, totals, or cards here.
    - Useful context: US-010 replaces names in the AccountBlock slot. US-013/014/027 fill ParametersBar.
---

## 2026-09-04 22:28 - US-009 review
- Dropped the no-op Expense `clsx` class (`.block` already has `min-height: 0`). Block titles use `ACCOUNT_BLOCK_TITLE_KEY` instead of a `toLowerCase` cast. Refresh loader: `gcTime` 90d overflowed `setTimeout` to 1ms and GC'd the restore; `gcTime` is `Infinity`, persist `maxAge` stays 90d. `QueryPersistenceProvider` stays a route `Outlet` under `RequireAuth`. Persist stays always on; logout/IndexedDB wipe is US-039.
- **Learnings for future iterations:**
    - Patterns discovered: every flex AccountBlock gets `min-height: 0`; do not special-case Expense in JS until its CSS actually differs. Map `AccountType` → i18n key instead of stringly `toLowerCase`. Query provider is still `RequireAuth` → provider + `Outlet` → `AppLayout`, not around `App`.
    - Gotchas encountered: `.expense { min-height: 0 }` duplicated `.block`. Do not wrap `App`, add KeepAlive, RestoreGate, or restore-before-render for the loader — the fix is `gcTime: Infinity`. `setTimeout` max is ~24.8d.
    - Useful context: ParametersBar stub slots stay explicit copies until US-014; keep `!data` as a separate tree without the bar. US-012 can add Expense-only styles when the grid actually differs.
---
